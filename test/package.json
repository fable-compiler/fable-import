{
  "private": true,
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "test-watch": "jest --watchAll"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^22.0.4",
    "jest-fable-preprocessor": "^1.3.3"
  }
}
